const constants = {
    delimiters: {
        start: '{{',
        end: '}}'
    },
    empty: '',
    locale: 'en-US',

    // ------------ Review
    matchExpression: /{{[^{}]+}}/,
};

export default constants;